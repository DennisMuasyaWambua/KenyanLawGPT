# Used automatically by `npm run build` (Vite production mode).
# The frontend is served over HTTPS, so the API must be HTTPS too — this points
# at the API subdomain (Cloudflare DNS-only, Let's Encrypt cert on the origin).
VITE_API_BASE_URL=https://api.dennismuasya.com
